
spring:
  application:
    name: "spring-config-server"
  profiles:
   activie: 
  cloud: 
    config:
      server:
        native: 
          search-locations:  file:///spring-config-server/src/main/resources/config
          
server:
  port: 8071
